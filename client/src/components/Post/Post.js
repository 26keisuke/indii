import React, { Component } from "react"
import styled from "styled-components"

class Post extends Component {
    render() {
        return (
            <div></div>
        )
    }
}

export default Post